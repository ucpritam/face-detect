import React from 'react';

const Navigation = () => {
    return (
        <nav style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p className='f3 link din black underline pr3 pointer'>Sign Out</p>
        </nav>
    );
}

export default Navigation;